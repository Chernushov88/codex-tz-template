Стандарт підготовки ТЗ для Codex (Gulp4 / WP / Vue.js)
Цей документ — міст між розробником та AI (Codex, Copilot, Cursor, ChatGPT). Він адаптований під специфіку Fullstack WordPress та Frontend Vue.js розробки.

1. Протокол для AI (Інструкція для ChatGPT/Gemini/Claude)
   Якщо цей файл завантажено, ти стаєш "Architect Assistant". Твоя мета — зібрати контекст та видати ТЗ, яке зрозуміє кодова модель (Codex).

Твій алгоритм:

Ідентифікація стеку: Визнач, це задача під WordPress (PHP/Gulp), чи під Vue.js (SFC, Composition API, Vite/Webpack).

Інтерв'ю: Став питання з розділу 3, фокусуючись на файловій структурі (наприклад, де лежить тема WP або компоненти Vue).

Генерація: Сформуй ТЗ за шаблоном з розділу 4.

Оптимізація: На запит "стиснути" — прибери розлогі пояснення, залишивши суху технічну специфікацію (пріоритет для IDE-контексту).

2. Інструкція для розробника
   Закинь цей файл у чат.

Напиши: "Потрібно ТЗ для [нової фічі/рефакторингу] у проекті [WordPress/Vue]".

Дай відповіді на уточнюючі питання.

Скопіюй фінальний блок у AI/PROMPT.md або безпосередньо в Copilot/Cursor.

3. Крок 1. Збір контексту (Питання для інтерв'ю)
   3.1. Технічний контекст
   Який це проект?

WP Fullstack: Потрібна правка в functions.php, створення нового шаблону сторінки чи кастомного плагіна?

Vue.js: Це Single File Components (SFC)? Composition API чи Options API?

Gulp 4: Чи потрібно додавати нові таски в gulpfile.js або змінювати структуру src/?

Стилі: SCSS/SASS чи Tailwind? Де лежать вихідні файли?

3.2. Суть задачі
Мета: Що саме має робити код? (Наприклад: "Створити кастомний блок через ACF Pro" або "Зробити фільтрацію товарів на Vue").

Логіка: Які дані приходять на вхід (WP Query, REST API, Props) і що маємо на виході?

3.3. Обмеження
Legacy: Чи є старий код, який не можна чіпати?

Стиль: Дотримуватися WordPress Coding Standards чи стандартного ESLint для Vue?

4. Крок 2. Шаблон структури ТЗ (Результат)
   Нижче наведено структуру, яку AI має заповнити для тебе:

4.1. Project Context & Stack
Core: [WordPress 6.x / Vue 3 / Gulp 4]

Environment: [напр. LocalWP, Docker, чи чистий Apache/Nginx]

Key Paths: - Тема: wp-content/themes/my-theme/

Vue: src/components/ або src/views/

Assets: src/scss/, src/js/

4.2. Functional Requirements
Чіткий список дій (To-Do list для AI).

Опис взаємодії (напр. "Vue компонент отримує дані через wp_localize_script").

4.3. Technical Implementation Details
WP: Реєстрація CPT, додавання хуків add_action/add_filter.

Vue: Опис props, emits, store (Pinia/Vuex).

Gulp: Чи потрібно запускати watch або перезбирати dist.

4.4. Constraints & Code Style
Не використовувати jQuery (якщо актуально).

Використовувати BEM для класів.

Тільки ES6+ синтаксис.

5. Приклад готового ТЗ (Для орієнтиру)
   Задача: Створити секцію "Команда" на WordPress + Vue.js.

Контекст: WP тема на Gulp 4. Вихідні файли в src/.

Вимоги:

PHP: Створити inc/post-types.php, зареєструвати CPT members.

Vue: Створити компонент TeamGrid.vue, який завантажує дані через WP REST API.

Styles: Додати src/scss/components/\_team.scss, імпортувати в main.scss.

Gulp: Перевірити, що новий JS файл підхоплюється таском scripts.

Обмеження: Використовувати Composition API, стилі без використання !important.

6. Крок 4. Чек-лист перед відправкою Codex'у
   [ ] Вказано, де лежить src, а де dist?

[ ] Для WordPress: чи вказано префікс функцій (щоб уникнути конфліктів), вказано назви хуків або файлів шаблонів?

[ ] Для Vue: визначено версію (2 чи 3) та API (Composition/Options)?

[ ] Для Swiper/Axios: чи вказано, як імпортувати бібліотеку (через npm/модулі чи CDN)?

[ ] Чи зрозуміло, які файли Codex має створити "з нуля", а які — доповнити?
